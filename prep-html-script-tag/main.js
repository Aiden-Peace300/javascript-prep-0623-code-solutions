/* The prase "I have added a script file via the src attribute!" will be
  outputted to the console
  NOTE: this is why we use console.log();                            */
console.log('I have added a script file via the src attribute!');
